global:
  repoURL: https://github.com/tAIness/devops-project1.git

apps:
  backend:
    enabled: true
    namespace: mario-backend
    chart: infra/helm/backend
    values: {}          # optional chart values overrides
  web:
    enabled: true
    namespace: mario-web
    chart: infra/helm/web
    values: { }   # optional overrides

